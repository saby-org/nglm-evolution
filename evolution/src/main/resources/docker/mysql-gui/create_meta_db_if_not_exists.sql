-- The _meta databse is owned by the root user and used to bootstrap
-- all the other database migrations. Use with care and ONLY to bootstrap.
-- Do not store ANYTHING of use in it. It should have only one table:
-- flyway's DB migrations table.

CREATE DATABASE IF NOT EXISTS `_meta_gui`;
