apiVersion: 1

datasources:
- name: Messages
  org_id: 1
  type: elasticsearch
  access: proxy
  database: "datacube_messages"
  url: 'http://<_MASTER_ESROUTER_HOST_>:<_MASTER_ESROUTER_PORT_>'
  jsonData:
    timeField: "timestamp"
    esVersion: 70